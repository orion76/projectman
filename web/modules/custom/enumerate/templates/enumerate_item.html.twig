{#
/**
 * @file enumerate_item.html.twig
 * Default theme implementation to present Enumerate item data.
 *
 * This template is used when viewing Enumerate item pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_enumerate_item()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('enumerate_item') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>
